# MySQL: mysql://usuario:password@host:puerto/nombre_bd
# IMPORTANTE: el plan gratuito de Clever Cloud (donde vive esta base de datos)
# limita a 5 conexiones simultaneas por usuario. Sin "connection_limit", Prisma
# abre un pool de varias conexiones por proceso (num_cpus*2+1) y las deja
# abiertas en reposo — un solo servicio en Railway ya puede consumir 4 de las
# 5, y cualquier cosa concurrente (subir una foto + guardar una propiedad, dos
# agentes a la vez) agota el resto y hace fallar los guardados. Hay que fijar
# un limite bajo explicito tanto aqui como en la variable DATABASE_URL de
# Railway en produccion.
DATABASE_URL="mysql://root:password@localhost:3306/toledo21_crm?connection_limit=2&pool_timeout=15"

PORT=4000
CLIENT_ORIGIN="http://localhost:5173"
# URL publica desde la que se sirve esta API (para construir URLs de imagenes
# en el feed). En local puede dejarse vacio; en produccion, la URL real.
PUBLIC_BASE_URL=""

# --- Feed de propiedades (para el plugin Houzez Property Feed u otros) ---
# Token que debe incluirse como ?token=... al pedir /api/feed/properties.json|xml
FEED_ACCESS_TOKEN="cambia-esto-por-un-token-largo-y-aleatorio"

# --- Autenticacion ---
# Cambia esto por una cadena aleatoria larga antes de desplegar a produccion.
JWT_SECRET="cambia-esto-por-un-secreto-largo-y-aleatorio"
# Codigo que deben introducir los nuevos empleados para poder registrarse.
TEAM_INVITE_CODE="toledo21-2026"

# --- Proveedor de IA (opcional, fase 2 - valoracion y mensajes) ---
AI_PROVIDER="" # "anthropic" | "openai"
AI_API_KEY=""

# --- WhatsApp (opcional, fase 2 - automatizacion de seguimiento) ---
WHATSAPP_PROVIDER="" # "meta_cloud_api" | "twilio"
WHATSAPP_API_KEY=""

# --- Email de confirmacion al enviar el formulario de contacto publico ---
# Crea una cuenta gratuita en https://resend.com y copia tu API key aqui.
# Sin esto configurado, el lead se guarda igual en el CRM, pero no se envia
# el correo automatico de confirmacion (se avisa por consola).
RESEND_API_KEY=""
# Remitente del correo. Para usar tu propio dominio (recomendado, ej.
# "Toledo21 <hola@toledo21.com>") primero hay que verificarlo en Resend.
# Mientras tanto puedes dejar el remitente de pruebas de Resend:
RESEND_FROM_EMAIL="Toledo21 <onboarding@resend.dev>"
# URL publica de la web (para el logo y los enlaces dentro del correo).
WEBSITE_URL="https://crm-toledo21.vercel.app"
